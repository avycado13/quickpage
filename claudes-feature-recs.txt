 ▐▛███▜▌   Claude Code v2.1.150
▝▜█████▛▘  Sonnet 4.6 · Claude Pro
  ▘▘ ▝▝    ~/Code/quickpage

❯ what features to add?                                                         

⏺ Let me look at the current state of the project first.

  Listed 1 directory (ctrl+o to expand)

⏺ feature-ideator(Feature ideation for quickpage dashboard)
  ⎿  Done (9 tool uses · 33.6k tokens · 3m 26s)
  (ctrl+o to expand)

⏺ Here are the top feature ideas, ranked by impact vs. effort:

  High Impact, Low Effort (start here)

  1. Wire real Gmail API — scope string already exists in api.ts, just needs
  gmail.readonly added to OAuth scopes and swapping mockEmails for a real
  useQuery call
  2. Google Meet "Join" button in CalendarCard — the Calendar API already
  returns hangoutLink; the mapper just discards it. One type change + a button
  render
  3. Checkbox mutation in TodoCard — checkboxes are currently decorative; PATCH 
  /tasks/v1/lists/{list}/tasks/{task} makes them functional
  4. Card show/hide toggle — a gear icon in the header, hiddenCards set in
  localStorage, filter before rendering. DnD already handles variable-length
  card arrays

  Medium Impact

  5. Complete BookmarksCard — currently hardcoded GitHub button with no
  add/remove UI; add a form + favicon via google.com/s2/favicons
  6. "Due today" urgency badges — color-coded left borders on calendar/classroom
   items due today or in 2h. Pure rendering change, data already there
  7. Multiple Google Tasks lists — fetchTodos hardcodes items[0]; add a
  list-picker dropdown persisted to localStorage
  8. Token expiry UX — silent re-auth failures leave blank cards with no
  explanation; show a "Session expired" banner instead

  Power User

  9. Focus/Today mode — header toggle that switches to single-column view of
  only today's events, todos, and scratchpad
  10. Cmd+K command palette — jump to bookmarks, create a task, navigate — fits
  the new-tab pattern perfectly

  My top recommendation: start with wiring Gmail (2 scope string edits + swap
  mockEmails) — it unlocks a card that's currently showing fake data with
  basically no new infrastructure.

✻ Brewed for 3m 49s